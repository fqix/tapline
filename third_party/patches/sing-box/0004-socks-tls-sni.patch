diff --git a/service/taplineinspector/internal/proxy/proxy.go b/service/taplineinspector/internal/proxy/proxy.go
--- a/service/taplineinspector/internal/proxy/proxy.go
+++ b/service/taplineinspector/internal/proxy/proxy.go
@@ -77,6 +77,29 @@
 		},
 	}
 	r.tlsConfig = mitm.TLSConfig
+	mitm.TLSConfig = func(host string, pc *goproxy.ProxyCtx) (*tls.Config, error) {
+		name, port, err := net.SplitHostPort(host)
+		if err != nil || net.ParseIP(name) == nil {
+			return r.tlsConfig(host, pc)
+		}
+		// SOCKS clients may resolve the destination locally. Wait for SNI before
+		// choosing its certificate; an IP-only client still gets an IP certificate.
+		// GetCertificate preserves the ALPN configuration added by goproxy.
+		return &tls.Config{
+			MinVersion: tls.VersionTLS12,
+			GetCertificate: func(hello *tls.ClientHelloInfo) (*tls.Certificate, error) {
+				certificateHost := host
+				if hello.ServerName != "" {
+					certificateHost = net.JoinHostPort(hello.ServerName, port)
+				}
+				config, err := r.tlsConfig(certificateHost, pc)
+				if err != nil {
+					return nil, err
+				}
+				return &config.Certificates[0], nil
+			},
+		}, nil
+	}
 	proxy.OnRequest().HandleConnectFunc(func(host string, pc *goproxy.ProxyCtx) (*goproxy.ConnectAction, string) {
 		if !r.hasIngress(pc.Req) {
 			return goproxy.RejectConnect, host
diff --git a/service/taplineinspector/internal/proxy/engine_test.go b/service/taplineinspector/internal/proxy/engine_test.go
--- a/service/taplineinspector/internal/proxy/engine_test.go
+++ b/service/taplineinspector/internal/proxy/engine_test.go
@@ -13,7 +13,6 @@
 	"encoding/json"
 	"encoding/pem"
 	"errors"
-	"fmt"
 	"io"
 	"math/big"
 	"net"
@@ -65,8 +64,16 @@
 }
 
 func TestEngineHTTPAndHTTPSOverMemory(t *testing.T) {
-	for _, encrypted := range []bool{false, true} {
-		t.Run(fmt.Sprint("tls=", encrypted), func(t *testing.T) {
+	for _, test := range []struct {
+		name, destination, serverName string
+	}{
+		{"http", "example.com:443", ""},
+		{"tls-domain", "example.com:443", "example.com"},
+		{"tls-ip-with-sni", "127.0.0.1:443", "example.com"},
+		{"tls-ip-without-sni", "127.0.0.1:443", "127.0.0.1"},
+	} {
+		t.Run(test.name, func(t *testing.T) {
+			encrypted := test.serverName != ""
 			ctx, cancel := context.WithTimeout(t.Context(), 10*time.Second)
 			defer cancel()
 			in, writer := io.Pipe()
@@ -118,7 +125,7 @@
 				}
 			}()
 			source := netip.MustParseAddrPort("127.0.0.1:32123")
-			conn, err := engine.DialContext(ctx, source, "example.com:443", !encrypted)
+			conn, err := engine.DialContext(ctx, source, test.destination, !encrypted)
 			if err != nil {
 				t.Fatal(err)
 			}
@@ -129,7 +136,7 @@
 			if encrypted {
 				roots := x509.NewCertPool()
 				roots.AppendCertsFromPEM([]byte(identity.Certificate))
-				client := tls.Client(conn, &tls.Config{RootCAs: roots, ServerName: "example.com", MinVersion: tls.VersionTLS12})
+				client := tls.Client(conn, &tls.Config{RootCAs: roots, ServerName: test.serverName, MinVersion: tls.VersionTLS12})
 				if err = client.HandshakeContext(ctx); err != nil {
 					t.Fatal(err)
 				}
