diff --git a/service/taplineinspector/internal/ipc/wire.go b/service/taplineinspector/internal/ipc/wire.go
--- a/service/taplineinspector/internal/ipc/wire.go
+++ b/service/taplineinspector/internal/ipc/wire.go
@@ -42,10 +42,11 @@
 type RequestOptions struct {
-	Method  string          `json:"method"`
-	URL     string          `json:"url"`
-	Headers map[string]any  `json:"headers"`
-	CA      json.RawMessage `json:"ca"`
-	Cert    json.RawMessage `json:"cert"`
-	Key     json.RawMessage `json:"key"`
+	Method   string          `json:"method"`
+	URL      string          `json:"url"`
+	Headers  map[string]any  `json:"headers"`
+	CA       json.RawMessage `json:"ca"`
+	Cert     json.RawMessage `json:"cert"`
+	Key      json.RawMessage `json:"key"`
+	Insecure bool            `json:"insecure,omitempty"`
 }
 
 type Message struct {
diff --git a/service/taplineinspector/internal/upstream/tls.go b/service/taplineinspector/internal/upstream/tls.go
--- a/service/taplineinspector/internal/upstream/tls.go
+++ b/service/taplineinspector/internal/upstream/tls.go
@@ -46,6 +46,9 @@
 
 func upstreamTLS(options ipc.RequestOptions) (*tls.Config, error) {
 	config := &tls.Config{MinVersion: tls.VersionTLS12}
+	if options.Insecure {
+		config.InsecureSkipVerify = true
+	}
 	ca, err := pemBytes(options.CA)
 	if err != nil {
 		return nil, err
diff --git a/service/taplineinspector/internal/upstream/transport.go b/service/taplineinspector/internal/upstream/transport.go
--- a/service/taplineinspector/internal/upstream/transport.go
+++ b/service/taplineinspector/internal/upstream/transport.go
@@ -38,7 +38,7 @@
 	if h3 {
 		destination = http3Destination[0]
 	}
-	encoded, err := json.Marshal([]any{route, options.CA, options.Cert, options.Key, h2c, websocket, h3, destination})
+	encoded, err := json.Marshal([]any{route, options.CA, options.Cert, options.Key, options.Insecure, h2c, websocket, h3, destination})
 	if err != nil {
 		return nil, fmt.Errorf("encode transport settings: %w", err)
 	}
diff --git a/service/taplineinspector/internal/upstream/transport_test.go b/service/taplineinspector/internal/upstream/transport_test.go
--- a/service/taplineinspector/internal/upstream/transport_test.go
+++ b/service/taplineinspector/internal/upstream/transport_test.go
@@ -68,6 +68,15 @@
 			}
 		})
 	}
+	t.Run("insecure skip verify", func(t *testing.T) {
+		config, err := upstreamTLS(ipc.RequestOptions{Insecure: true})
+		if err != nil {
+			t.Fatal(err)
+		}
+		if !config.InsecureSkipVerify {
+			t.Fatal("upstream verification not skipped when Insecure is true")
+		}
+	})
 }
 
 func TestRequestTimingEarlyResponse(t *testing.T) {
